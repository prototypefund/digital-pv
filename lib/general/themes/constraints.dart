import 'package:flutter/material.dart';

mixin Constraints {
  static const BoxConstraints sliderConstraints = BoxConstraints(maxWidth: 319);
  static const BoxConstraints aspectTitleConstraints = BoxConstraints(maxWidth: 319);
  static const BoxConstraints textFieldConstraints = BoxConstraints(maxWidth: 500);
  static const BoxConstraints formSaveActionConstraints = BoxConstraints(minWidth: 500, maxWidth: 500);
}
