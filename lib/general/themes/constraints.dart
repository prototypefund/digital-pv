import 'package:flutter/material.dart';

mixin Constraints {
  static const BoxConstraints sliderConstraints = BoxConstraints(maxWidth: 319);
  static const BoxConstraints textFieldConstraints = BoxConstraints(maxWidth: 500);
}
